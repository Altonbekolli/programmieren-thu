#ifndef Analyze_H
#define Analyze_H

#include <string>

int analyze(int number);
int analyze(const std::string& str);
int analyze(const char* str);  // Neue Deklaration für const char*

#endif
